set timeout=1

menuentry '{{ iso_url | basename | splitext | first }} (PXE)' {
    linux vmlinuz \
        ip=dhcp \
        inst.ks=http://192.168.1.106/init-config/${net_default_mac}.ks
    initrd initrd.img
}
